# 服务监听端口
server:
  port: 8080
  # 对外可访问的基础地址（含协议与域名），用于回传 Webhook 触发 URL。
  # 留空则只返回相对路径 /api/webhook/:token，由前端按当前 host 拼接。
  public_url: ""

# JWT 配置
jwt:
  secret: "change-me-in-production-please-use-env-APIFORGE_JWT_SECRET"
  access_ttl_minutes: 15
  refresh_ttl_hours: 168

# 数据库配置：driver 可选 sqlite / pg / mysql
database:
  driver: sqlite
  # sqlite 默认本地文件；pg/mysql 填写对应 DSN
  dsn: "./data/apiforge.db"

# 代理配置
proxy:
  # 单次响应体上限（字节），防止大响应撑爆内存
  max_body_bytes: 10485760

# CORS 白名单（开发期前端可能跑在其它端口）
cors:
  allow_origins:
    - "http://localhost:5173"
    - "http://127.0.0.1:5173"
